"""Modelling layer.

base.py         — common interface (fit, predict_proba, predict_set).
baselines.py    — population base-rate, construction-stratified, ANM-CRI, LightGBM.
tabpfn_model.py — primary TabPFN-3 model with survival masking + multitask heads.
survival.py    — Weibull AFT reference Bayesian model.
"""
